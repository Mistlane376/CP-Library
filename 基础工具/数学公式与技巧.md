# 数学公式与技巧

## 运算符优先级（从高到低）

1. `()` 括号
2. `* / %` 乘除取余
3. `+ -` 加减
4. `<< >>` 移位
5. `< > <= >=` 比较运算符
6. `== !=` 相等判断
7. `&` 按位与
8. `^` 按位异或
9. `|` 按位或
10. `&&` 逻辑与
11. `||` 逻辑或
12. `=` 赋值

## 求和化简

$$i \bmod j = i - [i/j] \times j$$

## 整数取整

- 向下取整：`x / t`
- 向上取整：`(x + t - 1) / t`
